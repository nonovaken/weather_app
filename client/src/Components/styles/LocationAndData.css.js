import colors from './color.css';

export default {
    locationStyle: {
        fontSize: 27,
        fontFamily: 'Roboto, sans-serif',
        fontWeight: 300,
        color: colors.mainColor
    },
    infoStyle: {
        fontSize: 16,
        fontFamily: 'Roboto, sans-serif',
        fontWeight: 300,
        color: colors.subColor,
        marginTop: 2
    }
}